apiVersion: v1
kind: ConfigMap
metadata:
  name: podinfo-values
  namespace: podinfo
data:
  values.yaml: |
    ingress:
      enabled: true
      className: nginx
      hosts:
        - host: podinfo.example.com
          paths:
            - path: /
              pathType: Prefix
